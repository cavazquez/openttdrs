# Issues de la matriz de mapas aleatorios

Este registro conserva issues reproducibles aun cuando la sesión local no
tenga un token válido para abrirlos automáticamente en GitHub. Cada issue
tiene un criterio verificable y apunta a la evidencia; no se declaran como
"paridad" las pruebas que sólo demuestran que el cargador acepta un `.sav`.

| ID | Tema | Estado | Criterio de cierre / evidencia |
|---|---|:---:|---|
| **RMAP-001** | Generar una matriz progresiva de mapas aleatorios (64² → 512²) con semillas deterministas y artefactos aislados. | **Cerrado** | `scripts/random_map_parity.py`, matriz 64:8/128:4/256:2/512:1 y tests en `scripts/test_random_map_parity.py`. La corrida contiene 15 casos sin errores. |
| **RMAP-002** | Comparar el mapa generado por OpenTTD con el mapa que abre `openttdrs`, tesela por tesela y por bloques 4×4. | **Cerrado** | Hook `OPENTTDRS_RANDOM_MAP_*` + `world_raw_dumper` sobre el `.sav` real: 15/15 exactos, 0 teselas distintas y 0 bloques 4×4 distintos. |
| **RMAP-003** | Evitar que una imagen raster enorme o el escalado oculte divergencias del mapa. | **Cerrado** | La comparación primaria no rasteriza: valida 10 campos por tesela, cuenta diferencias y localiza bloques 4×4. La captura queda sólo como diagnóstico secundario. |
| **RMAP-004** | Reproducir el mapa aleatorio de OpenTTD con el mismo seed desde el generador procedural Rust. | **Abierto (P1, avance parcial)** | La portabilidad de TGP/RNG, la suma de octavas de costa sin normalizar, `FixSlopes`, costas, `water_borders`, bordes `MP_VOID` y la etapa pueblos/industrias ya está en el generador. El port de `GenerateClearTile` materializa rough/rocks con su recorrido diagonal y `m5`; `GenerateObjects` ya crea transmisor/faro vanilla, escala, separación, `ObjectID` nativo y pool `OBJS`. El flujo conserva un único stream y `world_raw_dumper` reproduce las 0x500 pasadas de paisaje sin adelantar la economía. El oráculo RMAP-013 prueba cada frontera: tras RMAP-022/RMAP-023, `landscape` y `clear` son exactos (0/4096 teselas y 0/256 bloques 4×4) en las seeds 64² `1330935378` y `1330935379`. RMAP-049 reemplaza la trama MVP por el constructor `DoCreateTown`; RMAP-051/RMAP-053 portan la limpieza vial y las dos alternativas básicas de `LevelTownLand`. La matriz fresca de `towns` queda en **132 teselas/24 bloques 4×4** para `1330935378` y es exacta (**0/0**) para `1330935379`; ello sólo cubre esa frontera y esas dos semillas. El cargador sigue exacto 15/15 y el generador mismo-seed continúa abierto. |
| **RMAP-005** | Contrato de sustrato y costa de `GenerateTrees`. | **Cerrado** | `CanPlantTreesOnTile` ahora rechaza fields/rocks, respeta `allow_desert`, admite sólo costa sin una única esquina elevada, usa `GetTileZ` para agrupación por altura y materializa `TreeGround::Shore` (`m1=Sea`, `m2=0xF0`) mientras limpia el estado non-flooding vecino. Tests unitarios cubren cada contrato. El oráculo 64²/seed `1330935378` vuelve a exportar y el cargador tiene 0/4096 diferencias. |
| **RMAP-006** | Diferencial completo de la fase de árboles vanilla aislada. | **Cerrado** | El hook pre/post + `DATE.random_state` compara tiles, bloques 4×4 y cada llamada admitida a `PlaceTree`. `tree_phase_parity.py` es exacto para temperate, arctic, tropic y Toyland en semillas `1330935378..=1330935381` de 64² y la semilla `1330935381` de 128²: 0 diferencias de tesela, bloque y traza en cada caso. Este cierre no afirma que el generador completo entregue la misma entrada preárboles; esa frontera queda en RMAP-004/RMAP-013. |
| **RMAP-007** | Oráculo de frontera pre/post `GenerateTrees`. | **Cerrado** | El hook versionado guarda ambos `.sav` y emite `tree-generation-trace`; `scripts/tree_phase_parity.py --size 64 --seed 1330935378` retoma el `DATE.random_state` real y valida el mapa por tesela/bloques 4×4 y cada llamada admitida a `PlaceTree`. Resultado: 0/4096 teselas, 0/256 bloques y 357/357 llamadas distintas. |
| **RMAP-008** | Estado RNG global `DATE` en interoperabilidad SAV. | **Cerrado** | `SavGame` importa `random_state[0..1]` de `DATE`, `GameState::from_sav_game` lo restaura y el escritor OTTN lo preserva. Tests cubren columnas incompletas, lectura y round-trip. El dumper usa ese estado para `--replay-trees`; no se infiere desde la semilla de creación. |
| **RMAP-009** | Límite de altura en refuerzo de árboles y PATS. | **Cerrado** | `construction.map_height_limit` se importa y reemite en `PATS`; el replay usa su valor efectivo (`0` automático → 30). `PlaceTreesRandomly` ahora escala `GetTileZ()*2` por `15 / map_height_limit`, igual que `tree_cmd.cpp`; tests cubren 15, 30 y automático, y RMAP-007 confirma el stream RNG y las 357 colocaciones exactas. |
| **RMAP-010** | Árboles árticos: línea de nieve, suelo nevado y teselas no plantables. | **Cerrado** | `SavGame`/`GameState` importan y reemiten `PATS.game_creation.snow_line_height`; el replay aplica `z > snow_line` ×3 antes de escalar por altura, como `tree_cmd.cpp`. `PlaceTree` conserva `IsSnowTile` (MAP3 bit 4), `RoughSnow` de 9 bits y rechaza `MP_OBJECT` aunque el fallback semántico sea grass. Unit tests y `tree_phase_parity.py` dan 0 teselas/0 bloques 4×4 distintos: semillas `1330935378..=1330935381` en 64² (402, 818, 1008 y 598 llamadas trazadas) y `1330935381` en 128² (3195). |
| **RMAP-011** | Árboles tropicales: zona, tipo y pase de rainforest. | **Cerrado** | El replay conserva `GetTropicZone` en los bits bajos de `MAPT`, selecciona normal/rainforest/cactus y hace no-op para el tipo inválido de desierto, preservando esos bits cuando `SetTileType` convierte a árbol. Ejecuta los `15000` intentos de rainforest escalados después de cada pase normal y la traza incluye las llamadas admitidas aunque no escriban. Exacto en seeds `1330935378..=1330935381` de 64² (743, 490, 733, 457 llamadas) y seed `1330935381` de 128² (2206): 0 teselas y 0 bloques 4×4 distintos. |
| **RMAP-012** | Árboles Toyland: validación de rango y stream completo. | **Cerrado** | Toyland no crea grupos ni pase rainforest; el rango `TREE_TOYLAND..+9`, los dos pases y el stream de trazas dan igualdad exacta en seeds `1330935378..=1330935381` de 64² (200, 300, 337, 142 llamadas) y seed `1330935381` de 128² (820): 0 diferencias de bytes, bloques 4×4 y traza. |
| **RMAP-013** | Oráculo por fases preárboles del generador procedural mismo-seed. | **Cerrado** | `generation_phase_parity.py` hace que OpenTTD exporte `world-raw` en memoria tras `landscape`, `clear`, pueblos, industrias, objetos y árboles; el candidato usa `world_raw_dumper --generate-until` en las mismas fronteras. En 64², seeds `1330935378` y `1330935379`, el primer corte divergente es `clear` (respectivamente 1440 teselas/209 bloques 4×4 y 686/163). El pipeline ya no confunde un `.sav` incompleto con la primera causa; RMAP-014 toma la comparación exacta anterior a `GenerateClearTile`. |
| **RMAP-014** | Frontera `GenerateLandscape` previa a `GenerateClearTile`. | **Cerrado** | `apply_landscape_with_rng` y `apply_clear_generation_with_rng` separan ambas fases sin cambiar mapa ni estado RNG frente al camino combinado; `world_raw_dumper --generate-until landscape` y el harness comparan ya ambos lados. En 64²/seed `1330935378`, la primera divergencia es `landscape`: 1177 teselas y 117/256 bloques 4×4; 1080 difieren en `m3`, 119 en `m5` y 20 en `m1`/`type`. No se atribuye ya el residuo a clear; RMAP-015 toma su causa más temprana. |
| **RMAP-015** | Pasadas `RunTileLoop` de `CreateRivers` dentro de `GenerateLandscape`. | **Cerrado** | `run_landscape_river_tile_loops` ejecuta 256 pasadas LFSR antes de clear, con `TimerGameTick::counter` fijo en 0 como hace OpenTTD; no adelanta calendario, economía ni entidades. El test cubre además el detalle de que la visita manual de tile 0 deja `1 + 15×256` teselas estabilizadas en 64². Contra el oráculo 64², seed `1330935378` baja `landscape` de 1177/117 bloques a 356/73; seed `1330935379` queda en 219/50. Los bytes restantes no son un scheduling pendiente: RMAP-016 los separa en la conversión de agua/costa. |
| **RMAP-016** | Conversión `ConvertGroundTilesIntoWaterTiles` y costa por pendiente. | **Cerrado** | Se reemplazó la heurística ortogonal por el contrato de `water_cmd.cpp`: sólo `MP_CLEAR` con `z=0`, `MakeSea` plano, `MakeShore` de una esquina y `_flood_from_dirs` para pendientes compuestas; el marco `MP_VOID` se materializa antes de decidir costas y `MakeWater`/`MakeShore` reinician los bytes crudos como el original. Tests cubren agua/costa y pendientes `SLOPE_EW` sin direcciones. Las alturas crudas coinciden en ambas semillas; el residuo que quedaba tras el primer port se aisló y resolvió en RMAP-017, sin atribuir al conversor los ríos sintéticos ni `m3`. |
| **RMAP-017** | Conservación de costas compuestas durante `TileLoop_Water`. | **Cerrado** | El diagnóstico por las cuatro alturas comprobó que `ConvertGroundTilesIntoWaterTiles` ya decidía igual que C++: el error era que Rust secaba incondicionalmente toda costa compuesta. La rama `FLOOD_DRYUP` ahora revisa `_flood_from_dirs` y conserva la tesela si llega a `FLOOD_ACTIVE`, igual que `water_cmd.cpp`; tests cubren tanto conservar como secar. `landscape` baja de 174/62 a **21/7** bloques (seed `1330935378`) y de 115/38 a **40/11** (seed `1330935379`). Ya no hay `water→clear` de costa; los 20/39 cambios de tipo restantes son ríos sintéticos (`m1=0x51`) y un único `m3` por semilla, separados en RMAP-018/RMAP-019. |
| **RMAP-018** | Selección, trazado y ensanchamiento exactos de `CreateRivers`. | **Abierto (P1, avance parcial)** | RMAP-020 eliminó el fallo inmediato: en 64² ya no deja los 20/39 `WaterClass::River` sintéticos y las dos fronteras `landscape` son exactas. RMAP-022 reemplazó la fuente hash por intentos `RandomTile`, espiral 8×8, `FindSpring` y una exploración BFS hasta la frontera de RNG; el stream llega igual a `GenerateClearTile` en las dos seeds de control. El constructor final aún usa descenso simplificado: faltan el BFS/terminus que muta terreno, YAPF, lagos/humedales, ensanchamiento y climas tropicales. Extender el oráculo a tamaños/semillas donde el original sí cree ríos y portar esas reglas. Criterio: traza/contaje de pozos y rutas comparable, sin transiciones extra ni faltantes en la matriz 64²→512², antes de cerrar. |
| **RMAP-019** | Bit `MAP3` non-flooding residual de las pasadas post-río. | **Cerrado** | La hipótesis de cursor LFSR era falsa: el único `m3=1` extra de cada seed era consecuencia del río sintético vecino. Al impedir RMAP-020 que un pozo largo incompleto pinte agua, `landscape` coincide byte a byte en ambas muestras (incluido `m3`), por lo que no queda una corrección de tile-loop independiente. |
| **RMAP-020** | Admisión de pozos largos antes de materializar un río. | **Cerrado** | `CreateRivers` trata el único pozo de 64² como largo y exige `min_river_length × 4 = 64` Manhattan hasta agua. El port ahora separa pozos largos/cortos, preserva `min_river_length`/`river_route_random` en `WorldGenConfig` y acumula el trayecto antes de pintarlo; rechaza rutas sin terminación acuática o demasiado cortas. Tests cubren rechazo, aceptación y la cohorte de un pozo. Contra OpenTTD, seeds `1330935378` y `1330935379` quedan en **0 teselas** y **0 bloques 4×4** distintos en `landscape`. No cierra RMAP-018: aún faltan RNG, búsqueda espiral, BFS/YAPF, lagos/humedales y ensanchamiento para mapas donde sí hay ríos. |
| **RMAP-021** | Primera divergencia `GenerateClearTile` tras paisaje exacto. | **Cerrado** | La inspección directa de `_random` al entrar a `GenerateClearTile` confirmó el mismo estado que Rust (`[2994197790,149651104]` y `[4048847026,1940758386]`); el problema no era el río. El grupo rocky de Rust terminaba al primer salto inválido, pero C++ vuelve a sortear dirección hasta agotar su longitud. RMAP-023 porta ese bucle y su test; `generation_phase_parity.py` da `landscape` y `clear` exactos, 0/4096 teselas y 0/256 bloques 4×4, en ambas seeds. |
| **RMAP-022** | Frontera RNG de intentos de pozo de `CreateRivers`. | **Cerrado** | `carve_rivers` usa ahora los 512/128 intentos `RandomTile` de OpenTTD, recorre la espiral par 8×8 y aplica `FindSpring`; la exploración previa a la construcción conserva `RandomRange(queue.size())` cuando corresponde. El estado global comprobado justo antes de `GenerateClearTile` coincide con OpenTTD en las seeds 64² `1330935378` y `1330935379`. El trazado completo sigue abierto en RMAP-018. |
| **RMAP-023** | Reintentos de dirección de grupos rocky en `GenerateClearTile`. | **Cerrado** | `place_rock_group` no corta el grupo cuando una dirección cae en agua/borde/desierto: consume otro `Random()` y repite, como el `do/while` nativo. El test fuerza oeste inválido y sur válido y verifica tanto las rocas como el stream. Las dos fronteras `clear` 64² quedan exactas; RMAP-024 pasa a pueblos. |
| **RMAP-024** | Colocación y construcción inicial de pueblos de `GenerateTowns`. | **Abierto (P1)** | Con paisaje y clear exactos, la primera divergencia es towns. RMAP-025/RMAP-026/RMAP-028/RMAP-029/RMAP-031/RMAP-033/RMAP-035/RMAP-036/RMAP-037/RMAP-038/RMAP-039/RMAP-040/RMAP-041/RMAP-042/RMAP-043/RMAP-044/RMAP-045/RMAP-046/RMAP-047/RMAP-048 ya reproducen el total, primera fundación, sorteo de tamaño, bootstrap, las 87 iteraciones del walker, selección mutable, nivelado efectivo y veintinueve casas; RMAP-049 las ejecuta desde el constructor productivo. RMAP-051/RMAP-053 cierran la limpieza vial y el fallback descendente de `LevelTownLand`: la matriz fresca de `towns` queda en **132 teselas/24 bloques 4×4** para `1330935378` y es exacta (**0/0**) para `1330935379`. Los bytes alcanzan tipo, dueño, IDs, `m3`, `m5`, `m8` y alguna altura: todavía no se puede atribuir a industrias, objetos ni árboles. RMAP-027/RMAP-030/RMAP-032/RMAP-034 cubren secuencia multi-pueblo, reglas viales/casas y generalización. Cerrar sólo con `towns` exacto en ambas seeds y actualizar las fases posteriores. |
| **RMAP-025** | Total sugerido de pueblos y proporción de tierra de `GenerateTowns`. | **Cerrado** | `town_generation_target_count` porta `ScaleBySize(base) + (Random() & 7)` seguido de `ScaleByLandProportion`. `initial_land_count` trata costa como tierra y sólo descarta agua plana, igual que `CountLandTiles`. El fixture de la frontera clear de `1330935378` reproduce el total 3 y el avance de RNG `[1168016413,2955223551] → [1189259021,2830356610]`. No cierra RMAP-024: el total sólo determina intentos, no los pueblos fundados. |
| **RMAP-026** | Selector de una fundación `CreateRandomTown` y sus 20 intentos. | **Cerrado** | `RandomTile`, margen 12, distancia Manhattan 20, clear/árbol plano, cuadro 5×5 (12 válidas), límite de 20 y `FindNearestGoodCoastalTownSpot` ya se ejecutan sin consumir RNG adicional. Las fixtures de la frontera justo tras `GenerateTownName` reproducen `(47,23)` por costa y `(43,15)` por tierra, más los estados `[2945732258,1049486831]` y `[394065499,3120157675]` al entrar a `DoCreateTown`. La integración conserva ambas primeras fundaciones, el ID 0 y `TownLayout::Original`. El alcance termina antes de `DoCreateTown`; RMAP-027 conserva abierto el stream posterior y la secuencia completa. |
| **RMAP-027** | Secuencia multi-pueblo y frontera RNG de `DoCreateTown`. | **Abierto (P1)** | RMAP-049 ya sustituye la trama MVP y ejecuta el contador temporal, bootstrap y 88 llamadas dentro de `DoCreateTown`; para la primera ciudad de `1330935378` termina en la frontera exacta `[3221856382,229218699]` antes del siguiente `GenerateTownName`. RMAP-050/RMAP-052/RMAP-053 verifican ahora también la primera ciudad de `1330935379` desde el constructor: 78 casas temporales, población 1862, HouseID 16 en `(41,10)` y frontera `[3848068084,748657221]`. Falta comprobar la traza completa —seed `1330935378` `(47,23)→(16,51)→(28,36)`; seed `1330935379` `(43,15)→(22,51)→(45,40)→(17,18)→(32,27)`— con cada estado RNG. No cerrar por la primera coordenada ni por reducción visual. |
| **RMAP-028** | Sorteo `TSZ_RANDOM` y multiplicador inicial de ciudad en `DoCreateTown`. | **Cerrado** | Antes de crecer, `DoCreateTown` ejecuta `(Random() & 0xF) + 8` y multiplica por `initial_city_size=2` en la configuración vanilla. `initial_town_house_budget` lo consume al aceptar el sitio. La fixture C++ de la primera ciudad transforma `[2945732258,1049486831]` en `[3488465418,1441958355]` y 11→22; una ciudad normal cubre 19 y `[2271986047,1903929563]`. El contador es temporal y todavía no modifica el radio/crecimiento Rust: eso queda íntegramente abierto en RMAP-027. |
| **RMAP-029** | Bootstrap sin calles de `GrowTown` y bytes de carretera municipal. | **Cerrado** | Tras `TSZ_RANDOM`, el primer `GrowTown` de la seed `1330935378` recibe `[3488465418,1441958355]`, elige `(47,23)` y `GenRandomRoadBits` consume `1509800000`: `a=b=0`, `b^=2`, bits `0x05`; la frontera queda `[679301066,1509800000]`. `write_generated_town_road` materializa `MakeRoadNormal` municipal (`m1=15`, `m2=TownID`, `m3=240`, `m5=bits`, `m8=4032`), comprobado por unit test y por el `world-raw` de esa tesela. Sólo cubre la rama sin calle; la medición renovada queda 175/27 y 323/50, sin afirmación de mejora global. RMAP-030 conserva los siguientes pasos. |
| **RMAP-030** | Caminata `GrowTownAtRoad` y 87 iteraciones iniciales posteriores al bootstrap. | **Abierto (P1, avance parcial)** | RMAP-039/RMAP-040/RMAP-041/RMAP-042/RMAP-043/RMAP-044/RMAP-045/RMAP-046/RMAP-047/RMAP-048 portan y prueban el walker global de `TL_ORIGINAL` para las llamadas 2–88 de la primera ciudad: `RandomDiagDir`, selección/reintento de bits, `Chance16`, conexión parcial, `MakeRoadNormal`, control de caminos paralelos, retorno temprano, casas interiores de curvas, nivelado efectivo y veintinueve casas materializadas. RMAP-049 lo integra en `DoCreateTown` y su test vuelve a obtener `[3221856382,229218699]` antes del siguiente nombre en `1330935378`. RMAP-051 añade el subconjunto de `CleanUpRoadBits` y RMAP-053 porta la alternativa descendente de `LevelTownLand` si elevar toca infraestructura; ambos permiten reproducir la primera ciudad de `1330935379`. Sigue abierto por terraformación generalizada (pendientes empinadas/infraestructura no cubierta), puentes/túneles, estaciones y cobertura fuera de esas fixtures. RMAP-032 conserva callbacks/counters y las tentativas no vanilla; RMAP-027 la secuencia multi-pueblo. |
| **RMAP-031** | Pool vanilla y primera extracción ponderada de `TryBuildTownHouse`. | **Cerrado** | La primera llegada a `TryBuildTownHouse` de la ciudad inicial (`tile 46,24`, seed `1330935378`) entra con RNG `[1631607644,4263025919]`. GDB muestra que OpenTTD compone antes del filtro tardío un pool de 17 specs y peso 272; `RandomRange` deja resto 4, índice 9 y `HouseID 26`. `choose_generated_town_house_candidate` reproduce el pool, la extracción, `random_bits=157` y la frontera `[2387930541,1281562269]`. El orden es importante: año, casa única, pendiente y huella se comprueban tras extraer y quitar la opción, no al formar el pool. El alcance se limita al catálogo vanilla habilitado sin contadores saturados; reintentos, NewGRF, huellas y materialización permanecen abiertos en RMAP-032. |
| **RMAP-032** | Reintentos y materialización de `TryBuildTownHouse` / `BuildTownHouse`. | **Abierto (P1, avance parcial)** | RMAP-038 porta el núcleo vanilla de pool mutable: `RandomRange`, `swap_remove`, filtros tardíos de año/iglesia/estadio/pendiente y la validación de huella de RMAP-037, sin retirar del pool una opción antes de sortearla. La fixture C++ de la sexta tentativa prueba el descarte de casa 32 por fecha y la reextracción de 6 con las tres palabras RNG exactas. RMAP-040/RMAP-041/RMAP-043/RMAP-044/RMAP-045/RMAP-046/RMAP-047/RMAP-048 ya invocan el núcleo desde el walker `GrowTown` y materializan las veintinueve casas 1×1 de la primera ciudad hasta la llamada 88, incluidos HouseID 25 en la esquina de `(50,23)`, reintentos posteriores, tres obras con `MAP5=(contador, etapa)=(2,0)/(2,2)/(2,2)`, y HouseID 11 terminado sin sumar población: bytes, población, radio, `num_houses` y frontera previa al siguiente nombre coinciden. RMAP-052 añade HouseID 16, `m1=71`, en `(41,10)` de la ciudad de 144 llamadas y conserva 78/1862. RMAP-049 ejecuta ese camino y `UpdateTownGrowthRate` desde el constructor. Sigue abierto por callbacks de construcción NewGRF, contadores/clases saturables, flags de protección, huellas/reintentos no cubiertos y la validación antes de la segunda fundación. |
| **RMAP-033** | Bytecode y sorteo de construcción de una casa vanilla 1×1. | **Cerrado** | `MakeHouseTile` ya tiene representación explícita (`TownHouseSpec` / `make_town_house`): conserva el nibble bajo de `MAPT`, escribe `MAP1` random, `MAP2` TownID, flags de `MAP3`, obra en `MAP5`, refresh en `MAP6` y el `HouseID` de 12 bits. El `world-raw` nativo de la primera casa (`46,24`) prueba exactamente `height=3,type=48,m1=157,m2=0,m3=128,m4=0,m5=0,m6=0,m7=0,m8=26`; la fixture la reproduce. `BuildTownHouse` usa ahora `Chance16I` de 16 bits, no `RandomRange`: desde `[2387930541,1281562269]` da etapa completada, población 13 y `[3931740615,3932304260]`. RMAP-049 consume esas tres palabras desde `DoCreateTown`; la matriz actual queda en 137/25 y 392/65, sin presentar una mejora global. Multitile, filtros/reintentos y callbacks quedan abiertos. |
| **RMAP-034** | Validación y reubicación vanilla de huellas multitile en `TryBuildTownHouse`. | **Abierto (P1, avance parcial)** | RMAP-035 ya separó y cerró la escritura de las subteselas. RMAP-037 porta `CheckTownBuild2House`/`CheckTownBuild2x2House` a la expansión runtime: ambas orientaciones, las cuatro bases 2×2 en orden nativo, igualdad de `max Z`, pendiente prohibida y layout vial; una prueba usa un spec multitile para verificar que la construcción se mueve y conserva los IDs consecutivos. RMAP-040 conectó sólo una primera casa 1×1 al walker procedural; no es evidencia de que una huella multitile ya se decida/reubique en esa secuencia. Cerrar sólo al conservar bytes, población y stream RNG de cada variante dentro de la secuencia de generación; no sustituirlo por una casa 1×1. |
| **RMAP-035** | Escritura atómica de huellas vanilla de `MakeTownHouse`. | **Cerrado** | `TownHouseFootprint`/`make_town_house_footprint` valida todos los límites antes de mutar y conserva por subtesela `MAPH` y el nibble bajo de `MAPT`. Escribe `HouseID` en el orden nativo base, `+Y`, `+X`, `+X+Y`. Las fixtures `world-raw` de OpenTTD 128², ártico, 1975, seed `1330935378` cubren 1×2: `(84,52)` `66→67`, `m1=183`, pueblo 1; 2×1: `(105,62)` `74→75`, `m1=215`, pueblo 5; y 2×2 en obra: `(24,87)` `32,33,34,35`, `m1=221`, pueblo 4, `m5=19`. El test de borde prueba que no hay escritura parcial. No cierra RMAP-034 ni RMAP-032: todavía falta decidir/reubicar la huella desde el crecimiento real. |
| **RMAP-036** | Convención diagonal y orden de IDs en la expansión urbana runtime. | **Cerrado** | `town_expand` delega ahora sus pasos diagonales en `diag_dir_offset` (`NE=(-1,0)`, `SE=(0,+1)`, `SW=(+1,0)`, `NW=(0,-1)`) y su inversa se prueba explícitamente. `house_footprint_offsets` usa el mismo orden de `MakeTownHouse`: base, `+Y`, `+X`, `+X+Y`; la prueba de una 2×2 terminada verifica los IDs `80,81,82,83` en esas cuatro posiciones. Corrige la ruta runtime sin declarar paridad del generador: RMAP-034 sigue abierto hasta que el plan de `GenerateTowns` valide/reubique la huella antes de escribirla. |
| **RMAP-037** | Port aislado de validación/reubicación multitile a `town_expand`. | **Cerrado** | `resolve_town_house_footprint` reproduce los recorridos de `CheckTownBuild2House` y `CheckTownBuild2x2House`: 2×1/1×2 prueban primero la base original y luego la inversa; 2×2 intenta base, `+NW`, `+NW+NE`, `+NW+NE+SE`. `CheckBuildHouseSameZ` rechaza pendiente empinada, `NotSloped`, terreno no limpiable o `max Z` distinto; las reglas de cuadrícula 2×2/3×3 se verifican aparte. La ruta de generación ahora llama esa validación mediante RMAP-049, pero la fixture integrada sólo alcanzó casas 1×1. No cierra RMAP-034: faltan los oráculos de decisiones multitile dentro del stream. |
| **RMAP-038** | Núcleo vanilla de pool mutable para la generación de pueblos. | **Cerrado** | `choose_generated_town_house_candidate` ya construye el pool por disponibilidad, extrae con `RandomRange`, elimina por intercambio con la última entrada y sólo entonces evalúa año, casa única, pendiente y huella. La traza GDB de la primera ciudad 64²/seed `1330935378` fija la sexta tentativa: en `(46,27)` parte de `[3417675983,2894021268]`, descarta HouseID 32 por año, acepta 6, devuelve `random_bits=151` y deja `[3042269420,2388727447]`; el test reproduce pool 112/7 y dos extracciones. RMAP-049 lo llama desde los 88 `GrowTown`; RMAP-032 conserva callbacks/counters/materialización no cubierta. |
| **RMAP-039** | Walker vial global de las primeras llamadas de `GrowTownAtRoad`. | **Cerrado** | `grow_generated_town_road_once` usa el `Randomizer` global, no el LCG del runtime, y materializa los resultados viales cubiertos: dirección diagonal, reintentos de bit, `Chance16(1,6)`/`Chance16(1,4)`, bloque nuevo y bit parcial sobre carretera existente. La fixture 64²/seed `1330935378` reproduce las llamadas 2–5 tras el bootstrap: nuevas calles `(47,22)`, `(47,24)`, conexión `0x08` sobre `(47,23)` y `(47,25)`, con las fronteras RNG nativas. RMAP-049 lo conecta al constructor; RMAP-030/RMAP-032 continúan abiertos por alcance general. |
| **RMAP-040** | Primera materialización de casa dentro del walker global de `GrowTown`. | **Cerrado** | La llamada 10 de la primera ciudad 64²/seed `1330935378` recorre `IsRoadAllowedHere` sobre la pendiente NW de `(46,24)`, conserva sus `Chance16`, selecciona HouseID 26 con el pool mutable y ejecuta `BuildTownHouse`. La fixture comprueba el resultado `MP_HOUSE` (`m1=157`, TownID 0, `m3=128`, `m5=0`, `m8=26`), población 13, `num_houses=23` y estado RNG `[3931740615,3932304260]`, igual al oráculo GDB. RMAP-049 lo ejecuta desde `DoCreateTown`; callbacks, contadores/clases, protección, multitile y cobertura posterior permanecen en RMAP-030/RMAP-032/RMAP-034. |
| **RMAP-041** | Caminos paralelos y continuación de `IsRoadAllowedHere` en el walker de pueblos. | **Cerrado** | `generated_is_neighbour_road_tile` porta el barrido lateral/backward de `IsNeighbourRoadTile` y exige el bit vial que mira al eje. La rama sin carretera consulta `IsRoadAllowedHere` tanto en la tesela a escribir como en la continuación; conserva su azar de pendiente y permite el tramo recto junto a una casa cuando la continuación se rechaza. La fixture 64²/seed `1330935378` reproduce las llamadas 11–14: calle `(47,26)`, casa 16 en `(48,22)` (`m1=56`, población 108), calle `(46,23)` y calle `(47,27)`, con las fronteras `[2945147987,1759293208]`, `[996756625,699117934]`, `[1322664340,2304059721]` y `[3624132389,4014754631]`. RMAP-049 integra ese tramo; RMAP-030/RMAP-032 siguen abiertos por cobertura posterior y generalización. |
| **RMAP-042** | Orden de validación/curva de la entrada `MP_CLEAR` en `GrowTownInTile`. | **Cerrado** | La traza GDB de la llamada 15 de la primera ciudad 64²/seed `1330935378` demuestra que, tras llegar a `(45,23)`, OpenTTD consume `Chance16(1,6)`, rechaza `IsRoadAllowedHere` y termina la búsqueda sin sortear `Chance16(1,4)` ni una dirección curva. El walker conserva ese orden (entrada antes de curva/continuación). La regresión reconstruye las llamadas 2–14, exige `None` en la 15 y fija la frontera RNG `[15256948,1831888115]`; RMAP-049 lo ejecuta desde `DoCreateTown`. RMAP-030 sigue abierto por las variantes fuera de esta traza. |
| **RMAP-043** | Tercera casa 1×1 después de `SearchStopped` dentro del walker. | **Cerrado** | Desde la frontera de RMAP-042, la llamada 16 vuelve a atravesar cuatro calles y materializa HouseID 24 en `(46,26)`. La traza GDB fija `random_bits=56`, población `123`, `num_houses=25` y RNG final `[315420011,1693018963]`; la fixture reproduce los bytes `m1=56`, `m3=128`, `m8=24` y esa frontera exacta. RMAP-049 ejecuta esta secuencia desde el constructor; RMAP-030/RMAP-032 siguen abiertos para reintentos y variantes generales. |
| **RMAP-044** | Casa interior de curva y tramo repetido de `GrowTown` 17–29. | **Cerrado** | La primera divergencia posterior fue la llamada 26: con `ROAD_E` en `(50,23)` y el bit `ROAD_NE`, OpenTTD no sigue la vía sino que pone la casa interior HouseID 25 en `(51,22)`. `generated_town_corner_house_tile` porta las cuatro combinaciones `ROAD_N/E/S/W` con los offsets `TileAddByDir`, y mantiene `HasTileWaterGround`/límite antes de la lotería. La fixture GDB reproduce las llamadas 17–29 —casas, dos `None` y calles— y termina en `[2375691898,265796979]` (entrada de la 30), con 33 casas y población 470. El scope queda cerrado sólo para ese tramo; RMAP-030 sigue abierto desde la llamada 30 y RMAP-032 para todos los reintentos/callbacks restantes. |
| **RMAP-045** | Ventana de oráculo `GrowTown` 30–44 tras la casa de curva. | **Cerrado** | La secuencia existente pasa esta ventana sin otra regla nueva: cuatro retornos sin construcción, cinco cambios viales y seis casas 1×1. La regresión mantiene en una misma cadena el RNG/entidades y termina tras la llamada 44 con 39 casas, población 875 y frontera `[2310750700,2493436935]` (entrada de la 45), igual a GDB. RMAP-049 la ejecuta desde el constructor; RMAP-030 conserva las variantes generales. |
| **RMAP-046** | Nivelado de terreno y ventana `GrowTown` 45–60. | **Cerrado** | GDB aisló la primera diferencia en la llamada 56: `LevelTownLand(49,26)` se ejecuta antes de `IsRoadAllowedHere` y aplana la pendiente que permite la carretera `0x0A`. `level_generated_town_land` reproduce la alternativa ascendente de `TerraformTownTile` sobre terreno recién generado; la regresión continua verifica las llamadas 45–60, calles `(47,18)`, `(47,20)=0x0D`, `(49,26)=0x0A`, seis casas 1×1 y las obras HouseID 25/6 con `(contador, etapa)=(2,0)/(2,2)`. Termina con 45 casas, población 1009 y RNG `[1638330032,3483907467]` (entrada de la 61), igual al oráculo. RMAP-049 integra la ventana; RMAP-030 conserva otras variantes de terraformación. |
| **RMAP-047** | Ventana de oráculo `GrowTown` 61–72 y casa sin población. | **Cerrado** | La misma regresión continua cubre seis retornos, cuatro calles `0x0A` y dos casas 1×1 de la primera ciudad. HouseID 11 en `(49,27)` queda terminado (`MAP3` bit 7) pero no modifica la población, mientras HouseID 39 en `(45,19)` agrega 35 habitantes; ambos bytes y fronteras RNG se verifican junto con las calles. Tras la llamada 72 quedan 47 casas, población 1044 y RNG `[4159791342,1546280518]` (entrada de la 73), igual a GDB. RMAP-049 cubre su ejecución productiva; RMAP-030/RMAP-032 siguen abiertos por callbacks y generalización. |
| **RMAP-048** | Ventana final `GrowTown` 73–88 y frontera al siguiente pueblo. | **Cerrado** | Cuatro calles, cuatro casas 1×1 y ocho retornos completan las 87 iteraciones posteriores al bootstrap de la primera ciudad. La regresión cubre la obra HouseID 10 con `MAP5=18`, las extensiones parciales de `(50,26)` `0x0A→0x0B→0x0F`, y la casa final HouseID 25 en `(45,29)`. Tras la llamada 88 quedan 51 casas, población 1181 y RNG `[3221856382,229218699]`, exactamente el estado GDB al entrar al siguiente `GenerateTownName`. RMAP-049 ejecuta esa frontera desde el constructor; RMAP-030/RMAP-027 quedan abiertos por multi-pueblo y otra fixture. |
| **RMAP-049** | Integrar el constructor `DoCreateTown` en la caminata vanilla ya probada. | **Cerrado** | Se eliminó la trama MVP: el constructor crea el `Town` temporal con su ID/layout, aplica `TSZ_RANDOM`, radio y bootstrap, ejecuta `x × 4` iteraciones con el mismo `Randomizer`, retira las casas temporales, actualiza la tasa de crecimiento y publica sólo pueblos con población. Conserva/revierte el mapa en fallos y respeta rectángulos preservados. El test productivo de `1330935378` termina con 29 casas, población 1181 y RNG `[3221856382,229218699]`. La matriz fresca mejora esa seed de 175/27 a 137/25 bloques, pero empeora `1330935379` de 323/50 a 392/65; RMAP-051 la reduce a 368/57 sin cerrar RMAP-024/RMAP-027/RMAP-030. |
| **RMAP-050** | Oráculo e implementación de la primera ciudad de `1330935379`. | **Cerrado** | GDB confirma que `(43,15)` es ciudad, recibe presupuesto temporal 36 y ejecuta 144 llamadas a `GrowTown`. RMAP-051 limpia la salida vial y RMAP-053 reproduce el fallback de `LevelTownLand`: la ejecución productiva `DoCreateTown` termina con 78 casas temporales, población 1862, HouseID 16 (`m1=71`) en `(41,10)` y RNG `[3848068084,748657221]`, igual a C++. La matriz fresca no regresa la semilla `1330935378` (132/24) y deja `1330935379` exacta (0/0) en `towns`. La secuencia de los pueblos posteriores sigue abierta en RMAP-027. |
| **RMAP-051** | Limpiar salidas de `GrowTown` que terminan contra una tesela no conectiva. | **Cerrado** | Se porta el subconjunto de `CleanUpRoadBits` aplicable durante `GenerateTowns` después de componer la máscara vial: elimina ramas hacia casas, agua real, borde y tipos no conectivos, y conserva clear/árbol/carretera y costa transitable. La llamada 84 de la segunda seed ahora convierte `(44,13)` de `0x0A` a `0x02`; la llamada 90 ya retorna sin consumo extra y la regresión fija sus RNG `[3904639598,2282438850]` y el posterior a la 144 `[3848068084,748657221]`. La matriz fresca conserva 137/25 en `1330935378` y mejora `1330935379` de 392/65 a **368 teselas/57 bloques 4×4**. Puentes, túneles, estaciones y variantes runtime permanecen en RMAP-030. |
| **RMAP-052** | Casa y población perdidas en la llamada 134 de la primera ciudad de `1330935379`. | **Cerrado** | Tras RMAP-051, GDB mostró que la llamada 134 debía llegar a `TryBuildTownHouse` en `(41,10)` (tile 681), `HouseID 16`, `random=71`, y pasar de 73/1462 a 74/1557. La causa fue el nivelado previo de `(39,13)`: Rust elevaba vértices compartidos con calles y convertía `SLOPE_N` en `SLOPE_NE`, por lo que tomaba una vía en vez de la casa. RMAP-053 porta el fallback descendente; el test productivo desde `DoCreateTown` conserva la casa, 78/1862 y `[3848068084,748657221]`. |
| **RMAP-053** | Fallback de `LevelTownLand`/`TerraformTownTile` cuando elevar toca infraestructura. | **Cerrado** | Se modela el almacenamiento de las esquinas W/S/E/N y la validación de las cuatro teselas que comparten cada vértice. En terreno básico, `level_generated_town_land` intenta elevar las esquinas bajas y, si una calle bloquea la operación, baja las altas como OpenTTD. La regresión aislada verifica la pendiente NE y dos calles bloqueantes; la integración de 144 llamadas reproduce la casa 16 de RMAP-052. Pendientes empinadas, propagación y terraformación de infraestructura no se incluyen: quedan abiertas en RMAP-030. |

El avance de código de RMAP-004 deja un contrato reproducible para aislar
terreno: `OPENTTDRS_GENERATE_POPULATION=0` omite pueblos/industrias,
`OPENTTDRS_GENERATE_STARTUP_TICKS=N` reproduce los ciclos de tile loop y
`OPENTTDRS_WATER_BORDERS` permite fijar la máscara de costas. La comparación
completa continúa usando la configuración de partida nueva (población
activada por defecto), para no confundir un mapa jugable con un heightmap.

## Nota sobre issues remotos

Los IDs anteriores son estables dentro del repositorio y están listos para
copiarse a GitHub. La creación automática de issues remotos requiere renovar
la sesión de `gh` (`gh auth status` informa que el token actual es inválido);
no se inventaron números de issue ni se reutilizaron los de composición visual.
